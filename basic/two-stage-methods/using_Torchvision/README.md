# Faster R-CNN using Torchvision

1. `Torchvision` 을 사용해서 Faster R-CNN 사용해보기 (inference mode)
2. Faster R-CNN 구조 이해하기 (using `Torchvision`)
3. Faster R-CNN Fine-tuning (data class 바꾸기)
4. Faster R-CNN Fine-tuning Training

<br/>

***
## `Torchvision` 을 사용해서 Faster R-CNN 사용해보기 (inference mode)
``` bash
~$ python 01_Faster_R-CNN_using_Torchvision.py
```


<br/>


## Faster R-CNN 구조 이해하기 (using `Torchvision`)
```bash
~$ python 02_understanding_Faster_R-CNN.py
```


<br/>

## Fine-tuning(using `Torchvision`)
```bash
~$ python 03_Fine-tuning_Faster_R-CNN.py
```